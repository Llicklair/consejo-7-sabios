[project]
name = "consejo"
version = "0.1.0"
description = "El Consejo de los 7 Sabios — debate técnico estructurado entre 7 agentes con visiones opuestas."
requires-python = ">=3.11"
dependencies = [
    "rich>=13.7,<15",
    "rich-pixels>=3.0,<4",
    "pillow>=10.0,<12",
    "pygame>=2.5,<2.6",
]

[project.optional-dependencies]
tui = ["textual>=0.60"]
agents = ["anthropic>=0.40"]
dev = ["pytest>=8.0", "ruff>=0.5"]

[project.scripts]
consejo = "consejo.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/consejo"]

[tool.ruff]
line-length = 100
target-version = "py311"

# Phase D #17 deferred: Typer migration + bump requires-python to >=3.12
# (would unlock PEP 695 `type` aliases, @typing.override). Deferred because
# the current dev environment is 3.11.9 and the argparse CLI was heavily
# polished in this phase (slugify, bilingual errors, --problem alias).
